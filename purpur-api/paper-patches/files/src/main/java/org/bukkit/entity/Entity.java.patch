--- a/src/main/java/org/bukkit/entity/Entity.java
+++ b/src/main/java/org/bukkit/entity/Entity.java
@@ -1322,4 +_,116 @@
      */
     void broadcastHurtAnimation(@NotNull java.util.Collection<Player> players);
     // Paper end - broadcast hurt animation
+
+    // Purpur start - Ridables
+    /**
+     * Get the riding player
+     *
+     * @return Riding player
+     */
+    @Nullable
+    Player getRider();
+
+    /**
+     * Check if entity is being ridden
+     *
+     * @return True if being ridden
+     */
+    boolean hasRider();
+
+    /**
+     * Check if entity is ridable
+     *
+     * @return True if ridable
+     */
+    boolean isRidable();
+
+    /**
+     * Check if entity is ridable in water
+     *
+     * @return True if ridable in water
+     */
+    boolean isRidableInWater();
+    // Purpur end - Ridables
+
+    // Purpur start - API for any mob to burn daylight
+    /**
+     * Checks if the entity is in daylight
+     *
+     * @return True if in daylight
+     */
+    boolean isInDaylight();
+    // Purpur end - API for any mob to burn daylight
+
+    // Purpur start - Fire Immunity API
+    /**
+     * Checks if the entity is fire immune
+     *
+     * @return True if fire immune
+     */
+    boolean isImmuneToFire();
+
+    /**
+     * Sets if the entity is fire immune
+     * Set this to null to restore the entity type default
+     */
+    void setImmuneToFire(@Nullable Boolean fireImmune);
+    // Purpur end - Fire Immunity API
+
+    // Purpur start - Entity Visibility API
+    /**
+     * Sets whether this entity should be hidden from all players at the tracker level.
+     *
+     * @param noTrack true to hide from all players
+     */
+    void setGlobalNoTrack(boolean noTrack);
+
+    /**
+     * Returns whether this entity is globally hidden from all players at the tracker level.
+     *
+     * @return true if globally hidden
+     */
+    boolean isGlobalNoTrack();
+
+    /**
+     * Prevents this entity from being tracked/sent to the given player.
+     *
+     * @param player the player to hide from
+     */
+    void hideFromPlayer(@NotNull Player player);
+
+    /**
+     * Prevents this entity from being tracked/sent to all players in the collection.
+     *
+     * @param players the players to hide from
+     */
+    void hideFromPlayers(@NotNull java.util.Collection<? extends Player> players);
+
+    /**
+     * Allows this entity to be tracked by the given player again.
+     *
+     * @param player the player to show to
+     */
+    void showToPlayer(@NotNull Player player);
+
+    /**
+     * Clears all per-player and global no-track state for this entity.
+     */
+    void clearNoTrack();
+
+    /**
+     * Returns whether this entity is hidden from the given player at the tracker level.
+     *
+     * @param player the player to check
+     * @return true if hidden from this player
+     */
+    boolean isNoTrackedFor(@NotNull Player player);
+
+    /**
+     * Returns the set of player UUIDs that this entity is hidden from.
+     *
+     * @return unmodifiable set of UUIDs
+     */
+    @NotNull java.util.Set<java.util.UUID> getNoTrackPlayers();
+    // Purpur end - Entity Visibility API
 }
