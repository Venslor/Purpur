--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -1378,7 +_,12 @@
                 // CraftBukkit start - respect vanish API
                 if (visibleToPlayer && !player.getBukkitEntity().canSee(this.entity.getBukkitEntity())) { // Paper - only consider hits
                     visibleToPlayer = false;
                 }
                 // CraftBukkit end
+                // Purpur start - Entity Visibility API
+                if (visibleToPlayer) {
+                    visibleToPlayer = this.shouldTrackFor(player);
+                }
+                // Purpur end - Entity Visibility API
                 if (visibleToPlayer) {
                     if (this.seenBy.add(player.connection)) {
@@ -1400,6 +_,23 @@
             }
         }
 
+        // Purpur start - Entity Visibility API
+        private boolean shouldTrackFor(final ServerPlayer player) {
+            if (!this.entity.globalNoTrack
+                    && (this.entity.noTrackForPlayers == null || !this.entity.noTrackForPlayers.contains(player.getUUID()))) {
+                return true;
+            }
+            if (this.entity.isPassengerOfSameVehicle(player)) {
+                return true;
+            }
+            if (this.entity instanceof net.minecraft.world.entity.projectile.FireworkRocketEntity firework
+                    && firework.attachedToEntity == player) {
+                return true;
+            }
+            return false;
+        }
+        // Purpur end - Entity Visibility API
+
         private int scaledRange(final int range) {
             return ChunkMap.this.level.getServer().getScaledTrackingDistance(range);
         }
